name: apt_upgrade
description: System package updates via apt for Debian/Ubuntu systems
version: 1.0.0
type: system
deployment:
  platforms:
    - debian
    - ubuntu
  requires_root: true
  fastdeploy:
    enabled: true
    deploy_script: deploy.sh
    dynamic_steps: true
    requires_deploy_group: true
tags:
  - maintenance
  - security
  - updates