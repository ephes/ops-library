---
- name: env | Render chesslab environment file
  ansible.builtin.template:
    src: chesslab.env.j2
    dest: "{{ chesslab_env_path }}"
    owner: "{{ chesslab_user }}"
    group: "{{ chesslab_group }}"
    mode: "0600"
  no_log: true
  notify:
    - restart chesslab
