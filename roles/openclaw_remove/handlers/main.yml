---
- name: Reload systemd for OpenClaw remove
  listen: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Reload traefik for OpenClaw remove
  listen: reload traefik
  ansible.builtin.systemd:
    name: traefik
    state: reloaded
  failed_when: false
