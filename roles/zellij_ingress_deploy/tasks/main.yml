---
- name: Validate Zellij ingress configuration
  ansible.builtin.import_tasks: validate.yml

- name: Configure Traefik for Zellij ingress
  ansible.builtin.import_tasks: traefik.yml
  when: zellij_ingress_traefik_enabled | bool
