---
- name: Reload systemd for open webui remove
  listen: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart traefik for open webui remove
  listen: reload traefik
  ansible.builtin.systemd:
    name: traefik
    state: restarted
