---
- name: static site | Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: static site | Restart static site
  ansible.builtin.systemd:
    name: "{{ static_site_systemd_unit_name }}"
    state: restarted
