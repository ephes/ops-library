---
# Samba Time Machine - Handlers

- name: Restart smbd
  ansible.builtin.systemd:
    name: smbd
    state: restarted

- name: Restart nmbd
  ansible.builtin.systemd:
    name: nmbd
    state: restarted

- name: Reload smbd
  ansible.builtin.systemd:
    name: smbd
    state: reloaded
