---
- name: "Render MeTube environment file"
  ansible.builtin.template:
    src: metube.env.j2
    dest: "{{ metube_env_file }}"
    owner: root
    group: root
    mode: "0644"
  notify: restart metube
