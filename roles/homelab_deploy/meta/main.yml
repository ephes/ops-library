---
galaxy_info:
  role_name: homelab_deploy
  author: ops-library
  description: Deploy the Homelab Django application with dual router Traefik authentication
  license: MIT

  min_ansible_version: "2.15"

  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
    - name: Debian
      versions:
        - bullseye
        - bookworm

  galaxy_tags:
    - homelab
    - django
    - deployment
    - traefik
    - authentication

dependencies:
  - role: local.ops_library.homelab_shared
