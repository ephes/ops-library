---
- name: Ensure delve service is enabled and running
  ansible.builtin.systemd:
    name: "{{ delve_service_name }}"
    enabled: "{{ delve_service_enabled }}"
    state: "{{ delve_service_state }}"
    daemon_reload: true
