---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart delve
  ansible.builtin.systemd:
    name: "{{ delve_service_name }}"
    state: restarted

- name: reload traefik
  ansible.builtin.systemd:
    name: traefik
    state: reloaded
  failed_when: false
