---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart minio-offsite-replication-timer
  ansible.builtin.systemd:
    name: "{{ minio_offsite_replication_timer_name }}.timer"
    state: restarted
  when: minio_offsite_replication_enabled | bool
