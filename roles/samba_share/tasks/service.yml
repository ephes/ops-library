---
- name: Enable and start Samba services
  ansible.builtin.service:
    name: "{{ item }}"
    enabled: "{{ samba_share_service_enabled }}"
    state: "{{ samba_share_service_state }}"
  loop:
    - smbd
    - nmbd

