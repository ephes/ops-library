---
# Handlers for FastDeploy deployment

- name: reload systemd
  systemd:
    daemon_reload: yes

- name: restart fastdeploy
  systemd:
    name: "{{ fastdeploy_service_name }}"
    state: restarted
    enabled: yes

- name: restart traefik
  systemd:
    name: traefik
    state: restarted