---
- name: Validate Voxhelm ingress configuration
  ansible.builtin.import_tasks: validate.yml

- name: Configure Traefik for Voxhelm ingress
  ansible.builtin.import_tasks: traefik.yml
  when: voxhelm_ingress_traefik_enabled | bool
