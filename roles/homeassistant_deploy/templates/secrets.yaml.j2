# Home Assistant secrets file
# This file contains sensitive credentials
# Keep this file secure and never commit to version control

# UniFi Controller credentials for device presence detection
unifi_password: "{{ homeassistant_unifi_password_effective }}"

# MQTT credentials (if using MQTT broker)
# mqtt_password: "{{ mqtt_password | default('change-me') }}"

# Database password (if using external database)
# db_password: "{{ db_password | default('change-me') }}"

# Other integration passwords
# weather_api_key: "{{ weather_api_key | default('') }}"
