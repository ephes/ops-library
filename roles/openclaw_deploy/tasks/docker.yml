---
- name: docker | Ensure Docker Engine + Compose are installed
  ansible.builtin.import_role:
    name: local.ops_library.docker_install

- name: docker | Verify Docker Compose v2 is available
  ansible.builtin.command: docker compose version
  register: openclaw_docker_compose_version
  changed_when: false
