---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart metrics-endpoint
  ansible.builtin.systemd:
    name: "{{ metrics_endpoint_service_name }}"
    state: restarted
