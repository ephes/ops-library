---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart zfs-syncoid-replication-timer
  ansible.builtin.systemd:
    name: "{{ zfs_syncoid_replication_timer_name }}.timer"
    state: restarted
  when: zfs_syncoid_replication_enabled | bool
