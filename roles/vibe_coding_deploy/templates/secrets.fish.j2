# Managed by Ansible — vibe_coding_deploy role
# API keys and secrets — do not commit to version control
{% for key, value in vibe_coding_fish_env.items() %}
set -gx {{ key }} "{{ value }}"
{% endfor %}
