---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install SnappyMail packages
  ansible.builtin.package:
    name: "{{ snappymail_deploy_packages }}"
    state: present
