---
- name: Prepare test environment
  hosts: all
  become: true
  gather_facts: false

  pre_tasks:
    - name: Wait for systemd to be ready
      ansible.builtin.raw: systemctl is-system-running --wait || true
      changed_when: false

    - name: Ensure Ansible remote tmp exists
      ansible.builtin.file:
        path: /tmp/.ansible/tmp
        state: directory
        mode: "0700"

    - name: Gather facts
      ansible.builtin.setup:

    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600

  tasks:
    - name: Install PostgreSQL and dependencies
      ansible.builtin.apt:
        name:
          - postgresql
          - postgresql-contrib
          - python3-psycopg2
        state: present

    - name: Start PostgreSQL service
      ansible.builtin.service:
        name: postgresql
        state: started
        enabled: true

    - name: Create mail database
      become: true
      become_user: postgres
      community.postgresql.postgresql_db:
        name: mail
        state: present

    - name: Create mail database user
      become: true
      become_user: postgres
      community.postgresql.postgresql_user:
        name: mail
        password: "molecule-test-password"
        state: present

    - name: Grant database privileges to mail user
      become: true
      become_user: postgres
      community.postgresql.postgresql_privs:
        database: mail
        state: present
        privs: ALL
        type: database
        role: mail

    - name: Grant schema privileges to mail user
      become: true
      become_user: postgres
      community.postgresql.postgresql_privs:
        database: mail
        state: present
        privs: ALL
        type: schema
        obj: public
        role: mail

    - name: Install PHP and nginx (needed by postfixadmin_deploy role)
      ansible.builtin.apt:
        name:
          - php-cli
          - php-pgsql
          - php-mbstring
          - php-intl
          - php-gd
          - php-imap
          - php-fpm
          - nginx
        state: present

    - name: Start nginx service
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: true

    - name: Install Dovecot (for doveadm pw)
      ansible.builtin.apt:
        name: dovecot-core
        state: present

    - name: Create vmail user and group
      ansible.builtin.group:
        name: vmail
        system: true

    - name: Create vmail user
      ansible.builtin.user:
        name: vmail
        group: vmail
        system: true
        create_home: false
        shell: /usr/sbin/nologin

    - name: Create vmail base directory
      ansible.builtin.file:
        path: /tmp/vmail
        state: directory
        owner: vmail
        group: vmail
        mode: "0755"

