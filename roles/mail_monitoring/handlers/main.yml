---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart mail-monitoring-cleanup timer
  ansible.builtin.systemd:
    name: "{{ mail_monitoring_cleanup_service_name }}.timer"
    state: restarted
  when: mail_monitoring_cleanup_enabled | bool
