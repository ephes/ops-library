---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart dns-metrics-endpoint
  ansible.builtin.systemd:
    name: "{{ dns_metrics_endpoint_service_name }}"
    state: restarted

- name: restart dns-metrics-collector-timer
  ansible.builtin.systemd:
    name: "{{ dns_metrics_endpoint_timer_name }}.timer"
    state: restarted
