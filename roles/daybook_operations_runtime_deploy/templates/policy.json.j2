{# Schema 3: one delivery store for the machine, a worker pool, and the sources it
   can run. Written disabled by every action except `cutover`. #}
{{ {
  'schema': 3, 'enabled': daybook_operations_runtime_action == 'cutover',
  'api_url': daybook_operations_runtime_api_url, 'credential_path': daybook_operations_runtime_credential,
  'journal': daybook_operations_runtime_journal,
  'workers': daybook_operations_runtime_workers,
  'bindings': daybook_operations_runtime_bindings,
  'importer_policy': daybook_operations_runtime_importer_policy
} | to_nice_json }}
