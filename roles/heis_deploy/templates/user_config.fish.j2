alias bat="batcat"
alias fd="fdfind"

set -x PATH $PATH {{ heis_home }}/.local/bin
set -x PATH $PATH {{ heis_site_path }}/venv/bin
set -x VIRTUAL_ENV "{{ heis_site_path }}/venv"
set -x DJANGO_SETTINGS_MODULE "{{ heis_django_settings_module }}"
cd {{ heis_site_path }}
