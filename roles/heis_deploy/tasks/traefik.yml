---
# Configure Traefik for Heis

- name: Ensure Traefik dynamic config directory exists
  file:
    path: "{{ heis_traefik_config_dir }}"
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Create Traefik configuration for Heis
  template:
    src: traefik.yml.j2
    dest: "{{ heis_traefik_config_path }}"
    owner: root
    group: root
    mode: '0644'
  # Traefik automatically watches dynamic directory, no reload needed
