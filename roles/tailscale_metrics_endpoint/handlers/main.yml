---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart tailscale-metrics-endpoint
  ansible.builtin.systemd:
    name: "{{ tailscale_metrics_endpoint_service_name }}"
    state: restarted

- name: restart tailscale-metrics-collector-timer
  ansible.builtin.systemd:
    name: "{{ tailscale_metrics_endpoint_timer_name }}.timer"
    state: restarted
