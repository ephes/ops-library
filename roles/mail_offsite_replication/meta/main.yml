---
galaxy_info:
  author: ops-library
  description: Pull mail data from a remote host into an offsite ZFS dataset with staged DB/config snapshots
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm
  galaxy_tags:
    - mail
    - backup
    - replication
    - rsync
    - zfs

dependencies: []
